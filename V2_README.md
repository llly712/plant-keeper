# 国赛版固件说明

## 板子

- 主机：ESP32-S3，代码在 `host/host.ino`
- 从机：ESP32-C3，代码在 `slave/slave.ino`
- Arduino ESP32 Core：当前电脑安装的是 `3.0.4`
- 主机必须打开 PSRAM，模型和摄像头都要用

主机如果确实是 N16R8，在 Arduino IDE 里选：

- Board：ESP32S3 Dev Module
- Flash Size：16MB
- PSRAM：OPI PSRAM
- USB Mode：不要选 Hardware CDC and JTAG，GPIO19/20 已经给 LED 和舵机使用
- USB CDC On Boot：Disabled，下载和串口走板载 CH340

从机选：

- Board：ESP32C3 Dev Module
- USB CDC On Boot：Enabled，方便走原生 USB 看日志

## RS485 地址

主机是总线上唯一主站，从机和传感器都只应答，不主动发消息。

| 种植位 | 执行从机地址 | 三合一土壤传感器地址 |
|---|---:|---:|
| 1 | 2 | 1（保留出厂地址） |
| 2 | 3 | 34 |
| 3 | 4 | 35 |
| ... | ... | ... |
| 32 | 33 | 64 |

从机地址改 `slave/slave.ino` 里的 `myAddr`。每块从机地址不能一样。

传感器改地址使用功能码 06，写寄存器 `0x003E`。改地址时总线上一次只接一只默认地址为 1 的传感器，不然会一起应答。

例如把传感器从 1 改成 34：

```text
01 06 00 3E 00 22 CRC低 CRC高
```

## 从机寄存器

| 地址 | 内容 | 读写 |
|---:|---|---|
| 0 | 光照 ADC | 只读 |
| 1 | 模拟土壤 ADC | 只读 |
| 2 | 水泵状态 | 只读 |
| 3 | 肥料 A 泵状态 | 只读 |
| 4 | 肥料 B 泵状态 | 只读 |
| 5 | 补光灯亮度 0-255 | 读写 |
| 6 | 蜂鸣器，1 短鸣、2 长鸣 | 读写 |
| 7 | 水泵运行秒数，写 0 立即停止 | 读写 |
| 8 | 肥料 A 泵运行秒数 | 读写 |
| 9 | 肥料 B 泵运行秒数 | 读写 |

单次泵运行最长 60 秒。主机超过 2 分钟没有合法命令，从机会关掉三路泵和补光灯。

从机和土壤传感器支持断电重接。主机连续 3 次通信失败才显示离线，离线地址约每 3 秒重新探测。重新插入后不用重启主机。

## AI

当前主机引用 `host/plant_model.h`，实际是 `96x96x3 uint8` 五分类模型：

1. Healthy
2. Mildew
3. LeafSpot
4. Rust
5. Pest

OV2640 拍 QVGA JPEG，主机解码成 RGB，中心裁切 240x240，再缩放到模型输入。摄像头会跟着 360 度舵机拍左、中、右三个位置，最后保留健康概率最低的一张结果。

新版寻植按网页里的种植位标定表运行，不再固定左中右巡检。网页把花盆名称、执行从机地址和摄像头软件位置绑定，主机每 2 秒读取一次移动或巡检命令。

360 度连续旋转舵机没有绝对角度反馈。每次主机上电前应把摄像头手动放到中间基准位置，运行中不要手动扭动云台。要做到断电后也能自动找准位置，后续需要增加原点限位开关或更换 180 度位置舵机。

当前摄像头 `PWDN` 接 GPIO3，程序上电会先拉高关机，再拉低唤醒。GPIO10 是摄像头 XCLK，不是补光灯控制脚。

启动串口应看到：

```text
PSRAM: OK
Camera: 0x0
AI input: 96x96x3
RS485 online: 1
AI Healthy 80.0% 500ms
```

如果 `PSRAM: NO`，先改 Arduino IDE 的 PSRAM 选项，AI 不会启动。

如果出现 `Camera: 0x105 (ESP_ERR_NOT_FOUND)`，代表 SCCB 没读到 OV2640。先检查摄像头 3.3V、GND、排线方向、SIOD=GPIO4、SIOC=GPIO5 和 XCLK=GPIO10。这个错误发生在图像采集前，和模型无关。

如果出现 `FullyConnected per-channel quantization not yet supported`，运行：

```text
python convert_esp32_model.py
```

脚本会用 `plant_model_224.h5` 重新生成适合 ESP TFLM 的 `plant_model.h`。生成后重新编译烧录主机。

## 上电前注意

- 主机 GPIO48 的 AM2301 数据线上拉必须是 3.3V，现有资料写的是上拉到 5V，这个要改硬件
- 舵机单独用稳定 5V 供电，和主机共地，不要靠 ESP32 的 3.3V 带
- RS485 用总线接法，A 接 A、B 接 B，两端放终端电阻，所有板子共地
- 经典 MAX485 是一个单位负载，32 从机再加 32 只传感器会超过传统 32 节点能力。要全部挂满，收发器应换成 1/8 单位负载型号
- 摄像头 AI 调试时先只接 1 个从机和 1 个传感器，跑通后再逐个增加

OLED 实物按 SH1106 兼容驱动初始化。该屏控制器有 132 列显存，使用 SSD1306 驱动会出现右侧白边、左侧被裁掉。
